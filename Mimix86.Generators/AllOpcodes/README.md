These files contains all the opcodes known th Mimix86.
These are used to generate the `Opcode` "enum", and `OpcodeMap` collection class.

These files use the DSL described in [DslLib](../../DslLib/README.md).
